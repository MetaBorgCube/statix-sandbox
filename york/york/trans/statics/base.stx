module statics/base

imports signatures/lex-sig
imports signatures/base-sig

signature
  sorts TYPE 

rules  

  subtypeO : ID * TYPE * TYPE
  subtypesO maps subtypeO(*, list(*), list(*))
  
//  subtype : TYPE * TYPE
//  subtypes maps subtype(list(*), list(*))
  
  subs : scope * TYPE -> TYPE
  
  subss maps subs(*, list(*)) = list(*)
  
     
  subtypeO(_, T, T).
   
//  subtype(T, T).
  
  subs(s_subs, T) = T.
  
